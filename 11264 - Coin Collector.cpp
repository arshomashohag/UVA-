#include<cstdio>
#include<iostream>

#define lld long long int


using namespace std;

lld n,c[1000],i,j,k,with;
int t;

lld dp(lld ind,lld cur)
{

}


int main(void)
{
    scanf("%d",&t);
    while(t--)
    {
        cin>>n;
        for(i=0;i<n;i++)
            cin>>c[i];
        with=c[n-1]-1;
        k=dp(0,0);
    }

}
