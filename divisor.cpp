 #include<stdio.h>
#include<conio.h>

int main(void)
{
 int n_w,a,b,c;
 char str[40][40];
 
 scanf("%d",&n_w);
 
 for(a=0;a<n_w;a++)
  scanf("%s",&str[a][]);

  for(a=0;a<n_w;a++)
  for(b=0;str[a][b];b++)
  {

  }

}
